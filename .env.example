EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
# From RevenueCat → Project → API keys → Apple App Store. Public by design.
EXPO_PUBLIC_REVENUECAT_IOS_KEY=

# No EXPO_PUBLIC_STRIPE_PAYMENT_LINK: the in-app "subscribe on the web" button
# was removed (App Store guideline 3.1.1 forbids linking to an external purchase
# mechanism). Web subscriptions bought on the site are still honoured via the
# stripe-webhook Edge Function.
